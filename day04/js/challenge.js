// string.charAt(0).toUpperCase();

var getFirst = prompt("What is your first name?").trim();
var getLast = prompt("What is your last name?").trim();

alert() getFirst.charAt(0).toUpperCase() + getFirst.substr(1).toLowerCase();

alert(getFirst);
